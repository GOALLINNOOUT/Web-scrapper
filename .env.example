# Required: MongoDB connection string.
# Local example: mongodb://127.0.0.1:27017/web_intelligence
# Atlas example: mongodb+srv://USER:PASSWORD@cluster.mongodb.net/web_intelligence?retryWrites=true&w=majority
MONGODB_URI=mongodb://127.0.0.1:27017/web_intelligence

# Required: API port and browser origin for CORS.
PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# Runtime mode.
# Use development locally. Use production for clustered API workers.
NODE_ENV=development
PERFORMANCE_MODE=development
WEB_WORKERS=1

# Optional but recommended: enables BullMQ, page-level workers, Redis-backed rate limits, and robots cache.
# Local example: redis://127.0.0.1:6379
REDIS_URL=
START_WORKERS_IN_API=true

# Worker throughput controls.
# Production target is global throughput across all workers. Per-domain limits below still apply.
TARGET_CRAWL_PAGES_PER_SECOND=50
CRAWL_JOB_WORKER_CONCURRENCY=4
CRAWL_PAGE_WORKER_CONCURRENCY=200
DOMAIN_ENRICHMENT_CONCURRENCY=4
CRAWL_PAGE_WORKER_RATE=50
MONITORING_CHECKS_CONCURRENCY=4
MONITORING_SCHEDULER_INTERVAL_MS=60000
HTTP_AGENT_MAX_SOCKETS=200
CRAWLER_MAX_CONTENT_BYTES=5242880
CRAWLER_USER_AGENT=WebIntelligenceCrawler/1.0
# Optional comma- or pipe-separated pool. The crawler rotates these per request.
CRAWLER_USER_AGENT_POOL=
# Optional comma- or pipe-separated HTTP/HTTPS proxy URLs for outbound IP rotation.
# Example: http://user:pass@proxy1.example:8080,https://proxy2.example:8443
CRAWLER_PROXY_URLS=
CRAWLER_DOMAIN_CONCURRENCY=4
CRAWLER_DOMAIN_RATE_PER_SECOND=5

# JavaScript rendering is fallback-only. These controls keep heavy sites from consuming all page workers.
CRAWLER_RENDER_CONCURRENCY=4
CRAWLER_RENDER_RATE=10
CRAWLER_RENDER_TIMEOUT_MS=15000
CRAWLER_RENDER_QUEUE_MAX=500
CRAWLER_BLOCK_RENDER_ASSETS=true
# Optional: encrypt monitoring before/after/diff payloads at rest.
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
MONITORING_ENCRYPTION_KEY=

# Crawl limits.
MAX_CRAWL_DEPTH=10
MAX_PAGES_PER_JOB=10000
DATA_RETENTION_DAYS=90

# Robots.txt behavior.
# false = ignore robots.txt.
# true = fetch/cache robots.txt and skip disallowed paths.
CRAWLER_RESPECT_ROBOTS=false
PAUSED_CRAWL_RECHECK_MS=5000

# Redis cache TTLs for high-frequency reads.
CACHE_TTL_MONITORING_MS=3000
CACHE_TTL_CRAWL_MS=3000
CACHE_TTL_DATA_MS=10000
CACHE_TTL_DOMAIN_MS=15000
CACHE_TTL_METADATA_PREVIEW_MS=3600000

# API rate limits per 60-second window.
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_DEFAULT=100
RATE_LIMIT_PRO=1000
RATE_LIMIT_ENTERPRISE=10000

# Logger.
# Defaults to ./logs, with one JSON log file per day: YYYY-MM-DD.log
LOG_DIR=logs

# Webhook signing secret for HMAC-SHA256 payloads.
WEBHOOK_SECRET=

# Optional integrations for future production phases.
SENTRY_DSN=
CLERK_SECRET_KEY=
RESEND_API_KEY=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET=
S3_REGION=
